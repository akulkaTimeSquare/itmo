import numpy as np
from numpy.linalg import eig
from tabulate import tabulate  # pip install tabulate

# ┌────────────────────────────┐
# │    Параметры системы       │
# └────────────────────────────┘
J1 = 0.5733                # Суммарный момент инерции J₁
J2 = 5.721e-3               # Суммарный момент инерции J₂
J3 = 1.746e-5              # Суммарный момент инерции J₃

K12 = 4.2982e5             # Жёсткость связи между телами 1 и 2
K23 = 3.521e4             # Жёсткость связи между телами 2 и 3

# ┌────────────────────────────┐
# │ Формирование матрицы A     │
# └────────────────────────────┘
A = np.array([
    [0,        -1 / J1,      0,         0,          0         ],
    [K12,       0,          -K12,       0,          0         ],
    [0,        1 / J2,       0,        -1 / J2,      0         ],
    [0,         0,           K23,       0,         -K23       ],
    [0,         0,           0,         1 / J3,      0         ]
])

# ┌────────────────────────────┐
# │ Вычисление собственных значений и векторов │
# └────────────────────────────┘
eigenvalues, eigenvectors = eig(A)

# ┌────────────────────────────┐
# │   Вывод матрицы A          │
# └────────────────────────────┘
print("🟦 Матрица состояния A:\n")
print(tabulate(A, tablefmt="grid", floatfmt=".4f"))

# ┌────────────────────────────┐
# │   Вывод собственных чисел  │
# └────────────────────────────┘
print("\n🟩 Собственные числа (λ):\n")
for i, val in enumerate(eigenvalues):
    print(f" λ_{i+1} = {val:.2f}")  # Округленное представление

# ┌────────────────────────────┐
# │   Вывод собственных векторов│
# └────────────────────────────┘
print("\n🟥 Собственные векторы (v_i), каждый столбец — свой вектор:\n")
print(tabulate(eigenvectors, tablefmt="grid", floatfmt=".4f"))